Direto no LeetCode e pelo celular.

De tão fácil, fui fazer testes.

Modelo 1 (GoLang): 3ms. Horrível. Memória, horrível mas com a maioria. Tentei
utilizar um mapa, para ver se, nas repetições dos testes da plataforma,
agilizava os demais processos, e recursividade na própria função da solução.

Modelo 2 (GoLang): 1ms. Razoável. Memória, ainda horrível mas com a maioria.
Tirei a recursividade, mas mantive a questão do mapa.

Modelo 3 (GoLang): 1ms. Razoável. Memória, ainda horrível mas com a maioria.
Otimizei um "se", tirando-o.

Modelo 4 (Python): 56ms. Mediano. Nao vi menor que 15ms. Memória, excelente.
E o mesmo anterior, só alterando a linguagem e suas estruturas.

Modelo 5 (GoLang): 0ms. Não tinha como sere melhor. Memória, horrível. Usava
um vetor, no lugar de um mapa, cujo índice é aponta para a resposta. O vetor
é criado na primeira passada.

Parece que era indiferente montar um mapa para agilizar a resposta.

